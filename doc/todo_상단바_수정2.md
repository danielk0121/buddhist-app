## 작업 내용

- 상단바 우상단 햄버거 목록 버튼 삭제 (이전 작업에서 미완)
- 상단바 제목 변경: `경필 (經筆) - 경전 수필` → `경필 (經筆)` (줄바꿈) `경전 수필`
- 경전 상세 화면 우상단 설정 버튼 삭제

## 작업 결과

- App.jsx `AppToolbar` 제목을 JSX로 변경 — `경필 (經筆)` + `<br />` + `경전 수필`
- SutraDetail.jsx `Toolbar`에서 `right` 설정 버튼 prop 제거
